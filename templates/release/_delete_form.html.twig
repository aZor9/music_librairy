<form method="post" action="{{ path('app_release_delete', {'id': release.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ release.id) }}">
    <button class="btn btn-danger mt-1 mb-2">Delete</button>
</form>
