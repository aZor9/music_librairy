{% extends 'base.html.twig' %}

{% block title %}Release{% endblock %}

{% block body %}
    <h1>Release</h1>

<div class="row mt-5 mb-3">
    <div class="col-sm-4 col-md-3" >
        <img src="{{ release.thumbnailUrl}}" class="rounded">
    </div>
    <div class="col">
        <h1>
            {{ release.title }}
            <small class="text-secondary"> - {{ release.releasedAt ? release.releasedAt|date('d/m/Y') : '' }} </small>
        </h1>
        {# <p>
            {{ release.tracks | length }} track{{ release.tracks ? release.releasedAt > 1 ? 's':'' }} 
        </p> #}
            {# Type : {{ release.type }} #}
    </div>
</div>








    <a href="{{ path('app_release_index') }}" class="btn btn-info">back to list</a>

    <a href="{{ path('app_release_edit', {'id': release.id}) }}" class="btn btn-warning">edit</a>

    {{ include('release/_delete_form.html.twig') }}
{% endblock %}
